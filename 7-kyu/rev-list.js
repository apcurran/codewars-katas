"use strict";

/**
 * solution 1
 * time: O(n)
 * space: O(1) -- in-place reversal of arr
 * 
 * @param {number[]} arr 
 * @returns {number[]}
 */
function reverseList(arr) {
    return arr.reverse();
}

"use strict";

/**
 * 
 * @param {number} x 
 * @param {number} y 
 * @returns {number}
 */
function solve(x, y) {
    const upsideDownNumsMap = new Map([
        [1, 0],
        []
    ])
}

console.log( solve(0, 10) ); // 3
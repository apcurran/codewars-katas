"use strict";

// /**
//  * Create a function that takes an argument n
//  * and sums even Fibonacci numbers up to n's index in the Fibonacci sequence.
//  * @param {number} n
//  * @returns {number}
//  */
// function sumFibs(num) {
//     let evenNumsSum = 0;

//     for (let i )

//     const fibNum = fib(num);



// }

function fib(n) {
    if (n <= 1) return n;

    return fib(n - 1) + fib(n - 2);
}

// console.log( sumFibs(5) ); // 2
